const Sprachen = () => {
  // === DOM & VARS =======
  const DOM = {};

  console.log(DOM);
  //=== INIT ===========
  const init = () => {
    console.log('Test Sprache');
  };

  init();
};

export default Sprachen;
